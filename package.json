{
  "name": "osint-backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot server.ts",
    "build": "bun build ./server.ts --outdir ./dist --target bun",
    "compile": "bun build ./server.ts --compile --outfile ./dist/server"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6",
    "@types/node": "^25.9.3"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "express": "^5.2.1",
    "mongoose": "^9.7.0",
    "nodemailer": "^9.0.0",
    "zod": "^4.4.3"
  }
}
