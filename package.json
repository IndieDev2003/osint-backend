{
  "name": "osint-backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot index.ts",
    "build": "bun build ./index.ts --outdir ./dist --target bun",
    "compile": "bun build ./index.ts --compile --outfile ./dist/server"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "mongoose": "^9.7.0"
  }
}
